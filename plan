RPC后续开发计划：
1、修改所有涉及“CSS10”的注释、变量名、表单名为“CS10”（意为ClassSet10，与CS23相对应。）
2、修改所有CS10区域的鼠标提示文字信息，将英文职业/专精名改为双字职业/专精名，如MAGE改为法师。新的中文名直接覆盖对应的data表单。
3、创建刷新按钮，其作用为刷新当前团队的专精池名单及专精检查结果。
4、增加专精检查优先规则：
    1）设置为坦克职责的，禁止被载入治疗、输出专精表单中。
    2）设置为治疗职责的，禁止被载入输出、坦克专精表单中。
    3）设置为输出职责的，禁止被载入坦克、治疗专精表单中。
5、将调试模式复选框改为按钮，点击后切换调试模式。
6、增加调试模式按钮2，点击后print当前团队的专精池名单。格式为：“位置数据，玩家名-专精”，例如：“1-1，XXX-防骑；1-2，YYY...”。
按照每小队一行输出，每个玩家的专精池名单之间用分号隔开。
7、点击输出功能待设计。
8、自动回复功能：根据密语内容识别收到的密语发送者的职业/专精，并根据当前团队的专池名单发送自动回复：“已满”（职业/专精要求数量>1时）或“有了”（职业/专精要求数量为1）
9、增加一个按钮，其功能为修改当前配置检查要求为“宝库”或“副本”。CS10对应普通25人RAID，CS23对应宝库。
10、规范CS10规则和CS23规则，将其分别写入data表单中。
规则草案（待查）：
    CS10规则：
        1）普通25人RAID，每个职业/专精要求数量为1。
        2）每个职业/专精要求数量不能超过1人。
        3）每个职业/专精要求数量不能为0。
    CS23规则：
        1）宝库25人RAID，每个职业/专精要求数量为2。
        2）每个职业/专精要求数量不能超过2人。
        3）每个职业/专精要求数量不能为0。

11、明确检查逻辑，要求AI提供当前的检查逻辑拓扑。调整确认新的逻辑顺序，例如职责排他规则优先级、排他规则等。
12、梳理检查流程和逻辑，梳理检查池创建、修改、清理流程，确保在团队变化时能够及时更新检查池。